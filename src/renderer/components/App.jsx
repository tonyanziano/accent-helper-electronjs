import React from 'react';
import { SpecialCharacterButton } from './SpecialCharacterButton';

export const App = () => {
  return (
    <div>
      <SpecialCharacterButton text={':)'} />
    </div>
  );
};
